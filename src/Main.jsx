import React from "react"
import ReactDom from "react-dom/client";
import{BrowserRouter}from "react-router-dom";
import App from "./App";

ReactDOM.createRoot(document.getElementaryByld("root")).render(
    <BrowserRouter>
    <App/>
    </BrowserRouter>
);